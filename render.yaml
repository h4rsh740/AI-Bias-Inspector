services:
  - type: web
    name: ai-bias-backend
    env: docker
    plan: free
    region: oregon
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    envVars:
      - key: PORT
        value: 8080
      - key: GEMINI_API_KEY
        sync: false
